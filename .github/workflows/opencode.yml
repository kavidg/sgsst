name: OpenCode Agent
on:
  issues:
    types: [opened, edited]
  issue_comment:
    types: [created]

jobs:
  opencode-run:
    if: |
      contains(github.event.issue.body, '/opencode') || 
      contains(github.event.comment.body, '/opencode') ||
      contains(github.event.issue.body, '/oc') || 
      contains(github.event.comment.body, '/oc')
    runs-on: ubuntu-latest
    
    # Este bloque es CRUCIAL. Debe incluir id-token: write
    permissions:
      id-token: write
      contents: write
      pull-requests: write
      issues: write

    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0
          token: ${{ secrets.OPENCODE_GITHUB_TOKEN }}

      - name: Run OpenCode Agent
        uses: anomalyco/opencode/github@latest
        env:
          ANTHROPIC_API_KEY: ${{ secrets.ANTHROPIC_API_KEY }}
          GITHUB_TOKEN: ${{ secrets.OPENCODE_GITHUB_TOKEN }}
        with:
          model: anthropic/claude-3-7-sonnet
          token: ${{ secrets.OPENCODE_GITHUB_TOKEN }}