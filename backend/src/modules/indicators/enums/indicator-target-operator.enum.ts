/**
 * Operadores de meta para indicadores.
 *
 * GTE: greater than or equal (>=)
 * LTE: less than or equal (<=)
 * EQ: equal (===)
 * BETWEEN: between min and max (inclusive)
 */
export enum IndicatorTargetOperator {
  GTE = 'GTE',
  LTE = 'LTE',
  EQ = 'EQ',
  BETWEEN = 'BETWEEN',
}
